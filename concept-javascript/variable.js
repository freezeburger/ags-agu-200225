
var message = 'Au re var'

//const x; //Error PAS d'assignation

const name = 'Bob';
// name = 'bill' // Error

const user = {name:'Bull'}
user.name = 'Bill' // No Error still the same user

let open;
open = true;
open = false;

class User {}

class SuperUser extends User{}